---
const placeholderMessages = [
  {
    type: "assistant",
    content: "¡Hola! 👋 Soy el asistente virtual de Juan Carlos. Estoy aquí para responder tus preguntas sobre su experiencia, proyectos y habilidades. ¿En qué puedo ayudarte?"
  }
]
---

<div
  id="chatbot-window"
  class="fixed bottom-0 sm:bottom-28 left-1/2 transform -translate-x-1/2 w-full sm:w-[90%] h-[100vh] sm:h-[70vh] max-h-screen sm:max-h-[600px] bg-white/80 dark:bg-gray-800/90 rounded-none sm:rounded-2xl shadow-xl flex flex-col border-t sm:border border-black/10 dark:border-white/10 backdrop-blur-xl hidden z-50"
>
  <!-- Header -->
  <div class="p-4 border-b border-black/10 dark:border-white/10 flex justify-between items-center bg-white/50 dark:bg-gray-700/50 backdrop-blur-xl rounded-none sm:rounded-t-2xl">
    <div class="flex items-center gap-3">
      <div class="size-2 bg-green-500 rounded-full animate-pulse"></div>
      <h3 class="font-medium text-black/80 dark:text-white/80">Asistente Virtual</h3>
    </div>
    <button id="close-chatbot" class="hover:bg-black/5 dark:hover:bg-white/5 rounded-full p-2 transition-colors">
      <svg xmlns="http://www.w3.org/2000/svg" class="size-4" viewBox="0 0 24 24" fill="none" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </div>

  <!-- Messages Container -->
  <div id="messages-container" class="flex-1 overflow-y-auto p-4 space-y-4">
    {placeholderMessages.map(message => (
      <div class={`flex ${message.type === 'user' ? 'justify-end' : 'justify-start'}`}>
        <div class={`max-w-[85%] sm:max-w-[80%] rounded-2xl p-3 sm:p-4 ${
          message.type === 'user' 
            ? 'bg-yellow-500/90 text-white backdrop-blur-sm' 
            : 'bg-white/60 dark:bg-gray-700/60 text-black dark:text-white backdrop-blur-sm'
        } shadow-sm`}>
          {message.content}
        </div>
      </div>
    ))}
  </div>

  <!-- Input Area -->
  <form id="chat-form" class="p-3 sm:p-4 border-t border-black/10 dark:border-white/10 bg-white/50 dark:bg-gray-700/50 backdrop-blur-xl rounded-none sm:rounded-b-2xl">
    <div class="flex gap-2">
      <input
        type="text"
        id="chat-input"
        class="flex-1 rounded-xl border border-black/10 dark:border-white/10 px-4 py-2 sm:py-3 text-sm sm:text-base bg-white/70 dark:bg-gray-800/70 backdrop-blur-sm focus:outline-none focus:ring-2 focus:ring-yellow-500/50 text-black dark:text-white"
        placeholder="Escribe tu pregunta..."
      />
      <button
        type="submit"
        class="bg-white/10 dark:bg-white/5 hover:bg-white/20 dark:hover:bg-white/10 text-black dark:text-white rounded-xl px-4 sm:px-6 py-2 sm:py-3 transition-all duration-300 backdrop-blur-lg border border-black/10 dark:border-white/10 font-medium text-sm sm:text-base"
      >
        Enviar
      </button>
    </div>
  </form>
</div> 